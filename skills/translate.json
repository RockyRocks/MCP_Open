{
  "name": "translate",
  "description": "Translate text between languages",
  "prompt_template": "Translate the following text from {{source_lang}} to {{target_lang}}:\n\n{{input}}",
  "default_model": "gpt-4o",
  "default_parameters": {
    "temperature": 0.1,
    "max_tokens": 2048
  },
  "required_variables": ["input", "target_lang"],
  "system_prompt": "You are a professional translator. Maintain the original tone and meaning.",
  "rules": [
    "Preserve formatting and structure of the original text",
    "Use natural phrasing in the target language — avoid literal translations",
    "If source language is not specified, detect it automatically"
  ]
}
